// Implement Image Carousel Frontend System Design -
// Note -
// (Base Logic) -> Create a index useState and Show current Image with the help of Array current index.
// Left and Right Carousel Button functionality should be increasing and decreasing index of the images array.

// Implement Search AutoComplete -
// Note -
// (Base Logic) -> On the onchange on the text input field implement this function -> 
// const searchCountries = (text) => {
//     countries will be having the list of countries for example [{name: "Abhishek Verma"}]
//     const filteredCountries = countries.filter((country) => {
//       const regex = new RegExp(`${text}`, "gi");
//       return country.name.match(regex);
//     });
//     console.log(filteredCountries);
//     setCountriesSuggestion(filteredCountries);
//   };

// Implement Tic Tac Toe in React - Implemented in TicTacToe-React Folder
// Create a 3 X 3 Layout of Square box, onClick of the box pass the index of it to a function
// That function will populate that Square box and after populating checkWinner logic will be called

// Implement Timer in React - Implemented / All is based on use of setTimeInterval

// Implement table wise sum just like we do in Maths 
//     2 2 2 - These individual Item will be an Input fields
//   + 1 2 4
//     3 4 6

